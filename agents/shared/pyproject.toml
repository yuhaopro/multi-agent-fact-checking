[project]
name = "shared"
version = "0.1.0"
description = "Shared library for agents"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.0",
    "kafka-python>=2.3.0",
    "neo4j>=6.1.0",
    "pydantic",
]

[tool.setuptools]
package-dir = { "" = ".." }
packages = ["shared"]
